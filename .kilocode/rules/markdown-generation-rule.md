# Markdown Generation Rule

## Guidelines

- All markdown files must be generated by the `architect` mode.
- The `architect` mode is responsible for planning and creating the content of all markdown files.
- Other modes should not create or modify markdown files directly.
