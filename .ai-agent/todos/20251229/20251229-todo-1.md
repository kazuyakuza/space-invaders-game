- modify level end behaviour: instead of going to next level when all enemies are dead, new level must be load when all enemies red color are killed. The next level must loaded, and all the other enemies types must remain until they are killed.
- modify the enemy orange color behaviour: instead of shootting bullets randomly, it should shoot bullets in a pattern that targets the player's current position.
- fix bug: the enemy orange color bullets must traspass the other enemies, without affecting them, and must kill the player.
- modify enemy violet color behaviour: this enemy must have 10 times the enemyHealth's level config when it spawns.
- add feature: player have an amount of lives. it start with 3 lives. when its killed, the player must lose one life and respawn at the position where was killed immediately. If the player has no lives left, the game must end.
- add feature: show the player's current lives count on the screen.
